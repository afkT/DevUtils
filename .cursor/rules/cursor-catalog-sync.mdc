---
description: >-
  在本仓库对 .cursor/ 做会影响工作区可发现性或行为的变更后（含 rules、skills 及后续其它纳入
  .cursor/README.md 编目的路径），同步更新 .cursor/README.md：子树一览、表格与摘要须与仓库一致，
  条目顺序对齐文件系统，描述凝练准确。
alwaysApply: true
---

在本仓库中若你 **新增、删除或重命名** `.cursor/` 下已纳入或应纳入 **[`.cursor/README.md`](../README.md)** 编目的内容（例如 `rules/*.mdc`、`skills/**/SKILL.md`，以及将来出现的 `hooks`、其它约定目录或顶层说明文件），必须在同一轮或后续提交前 **更新该 README**：同步「当前子树一览」、各章节表格与说明，并视情况更新文末维护清单。不限于仅维护 `rules` 与 `skills` 两节——任何新的 `.cursor` 结构若需团队可见，应在 README 中增删对应小节。

**条目顺序**：README 内同一父目录下的表格行、分节编号（如 §2.1、§2.2）、目录树子项与并列列表，须与仓库中该目录的 **实际子项顺序** 一致（默认按路径/文件名字典序，与 `find … | sort` 或资源管理器在该目录下的列出顺序一致）。不得按主题重要性、使用频率或 YAML `name` 自行重排。

**描述风格**：编目表格中的说明列（如触发场景、摘要）及下文分条列表，应 **简短且准确**——一句说清核心能力与适用任务；避免整段照搬 SKILL/rule 的 YAML `description`、避免路径与实现细节堆砌，也不宜过短以致无法判断是否适用。表格列与 § 小节列表保持同一凝练标准。

## `skills/` 编目补充（互链与 reference 列）

对 **新增、删除或重命名** `skills/<目录>/` 时，除更新本 README 的目录树与 §2 表格外，还须：

1. **「伴随 reference」列**：该 Skill 目录下存在 `reference.md` 则表格填链到 `skills/<目录>/reference.md`；否则填 `—`。目录树子项须列出 `SKILL.md` 与 `reference.md`（若有），顺序与文件系统一致。
2. **Skill 互链**：在 `.cursor/skills/` 内检索指向其它 Skill 的相对路径（如 `../<skill-name>/SKILL.md`、`reference.md`）。重命名或删除目录后，**同一轮**修正所有仍引用旧目录名的链接；新增互链时目标目录须已存在。
3. **重命名自检**：对变更过的 `skills/**` 执行等价于 `grep -r '\.\./' .cursor/skills --include='*.md'`，逐条确认 `../` 链接仍有效；勿保留指向已删除目录或曾用目录名的路径。
