---
description: >-
  创建、重命名或重构 Agent Skill 时强制套用：统一规范化技能命名（小写 kebab-case、目录名与
  YAML name 完全一致、流程类用动名词短语、能力类用名词、避免 utils/helper/common 等模糊名），
  并约定文档分层结构（SKILL.md 放触发条件、执行流程与最少锚点；references/ 放大表、全量清单与
  会漂移的快照数据；scripts/ 放可执行代码；assets/ 放产出物素材）。当用户说「新建技能」「重命名
  技能」「拆分 SKILL」「整理 skill 目录结构」「技能命名规范」「SKILL.md 太长怎么拆」等，或在编辑
  .cursor/skills/ 下任意文件时，必须遵循本规则，即使用户没有显式点名"规范"二字。
alwaysApply: false
---

# 编写 Agent Skill（命名 + 目录/文档结构）

本规则把"如何给 Agent Skill 命名"与"如何组织 Skill 的文档与目录结构"合并为一套强制规范。
**在创建、大幅改写或拆分一个 Skill 时遵守本规则；日常业务任务不必强行套用。**

## 何时使用

- 新建一个 Skill，需要决定它的名字和目录结构。
- 重命名已有 Skill，或把旧的 snake_case / prompt 名称迁移为标准技能名。
- `SKILL.md` 变得过长、堆了大表，需要拆分到独立的参考文件。
- 评审一个 Skill 是否符合命名与结构规范。

## 核心原则：渐进式披露（Progressive Disclosure）

Skill 按三层加载，任何命名与结构决策都服务于"少占上下文、按需展开"：

1. **元数据**（`name` + `description`）——始终在上下文中，是触发的唯一依据（约 100 词）。
2. **`SKILL.md` 正文**——技能被触发时载入，**应保持在 500 行以内**。
3. **捆绑资源**（`references/` / `scripts/` / `assets/`）——按需读取；脚本可直接执行而不占用上下文。

---

# Part 1 · 命名规范化

创建、重命名或评审技能时，**必须在写入文件前先规范化技能名称**。

## 标识符规则

- **目录名必须与 YAML `name` 完全一致**——三者（目录名、技能名、`name` 字段）是同一个字符串。
- 只能使用小写 **kebab-case**：正则 `^[a-z0-9]+(-[a-z0-9]+)*$`，即只允许 `a-z`、`0-9`、`-`。
- 不允许下划线、空格、大写、非英文目录名、首尾连字符或连续连字符。
- `name` 长度 **不超过 64 个字符**。
- 不要使用含义模糊的裸 topic：`utils`、`helper`、`common`、`base`、`manager`、`misc`。
- 不要与运行环境的内置 Skill 重名。

## 命名族选择（先定族，再起名）

技能名分两大族，按技能性质二选一：

### A. 流程 / 过程 / 工作方法类 → 动名词短语（gerund，`verb-ing-…`）

当技能描述的是"一段要做的事 / 一套工作方法"时，用动词的 `-ing` 形式开头。

- 模式：`<verb-ing>-<object>[-with-<tool>]` 或 `<verb-ing>-<object>[-for-<purpose>]`。
- 示例：`authoring-agent-skills`、`triaging-incident-reports`、`writing-release-notes`、`migrating-build-config`、`reviewing-pull-requests`、`performing-dependency-audit`。

> 为什么用动名词：技能本质是"让 Agent 会做某事"，动名词让 `name` 与触发判断天然呼应（用户想做 X → 触发 X-ing 技能），也避免与工具名词混淆。拿不准用哪族时，**默认动名词**。

### B. 能力 / 格式 / 工具封装类 → 名词（短语）

当技能封装的是一种"对象 / 格式 / 工具能力"，且本身就是高辨识度名词时，用名词或名词短语，追求简短。

- 示例：`pdf`、`mcp-builder`、`changelog-generator`、`theme-factory`、`api-client`。
- 构建器 / 创建器用 `-builder` / `-creator` / `-generator` 后缀。

## 作用域 / 领域前缀（可选，按需）

当一套技能库里需要按领域归类时，可采用 `<scope-or-domain>-<specific-topic>` 模式，topic 通常为 2–4 个具体英文单词：

- 选一个最能具体描述产物的领域前缀（例如按构建、发布、界面、数据、导航、依赖、质量、基础设施、代码风格、协作流程等划分），**只用一个主前缀**。
- 与特定项目私有约定、基类、目录结构、封装组件、依赖配置、团队流程或生成代码契约绑定的技能，用 `project-` 前缀；若领域清晰，写成 `project-<domain>-<topic>`。
- 仅当某约定可在单个项目之外复用时，才使用裸领域前缀（不带 `project-`）。
- **不要**把仓库、文件夹、产品、团队或组织名称用作前缀，除非确实需要在一套共享技能库中区分多个项目族。

> 前缀只是可选的归类手段；它不改变"流程用动名词、能力用名词"的基本族归属。

## description 写法（触发的核心）

- **必须同时说明 WHAT（做什么）与 WHEN（何时使用）**——所有"何时使用"信息都放进 `description`，不要只写在正文。
- 包含用户可能说出的**触发词 / 口语化说法**，并写得**略带推动性**以对抗"漏触发"，例如："…… 当用户提到 X、Y 或 Z 时使用，即使没有显式要求。"
- `description` 长度控制在 **1024 字符以内**；富关键词便于检索。

## 旧名称迁移

- 从现有 prompt 名 / snake_case 转换时：把 `_` 改为 `-`，补上正确的族归属（动名词或名词）与可选前缀，**不要保留下划线**。
- 配套规则 / 文档若存在，应与技能使用**相同词干**（记录旧别名时除外）。

## 命名预检清单

- [ ] 目录名 == YAML `name`。
- [ ] 小写 kebab-case，且 ≤ 64 字符；无下划线 / 空格 / 大写。
- [ ] 已选定命名族：流程类=动名词短语，能力类=名词（短语）。
- [ ] topic 具体明确，不是 helper / common / utils / manager 等模糊名。
- [ ] （如使用前缀）只用一个主前缀；项目专用行为用 `project-…`。
- [ ] `description` 同时含 WHAT 与 WHEN，带触发词，≤ 1024 字符。

---

# Part 2 · 目录与文档结构

## 标准目录骨架（按需裁剪）

```
<skill-name>/
├── SKILL.md            (必须)  YAML frontmatter(name+description) + Markdown 正文
├── scripts/            (可选)  被运行：确定性 / 重复性的可执行代码
├── references/         (可选)  被阅读：按需载入上下文的深度文档、大表、全量清单
└── assets/             (可选)  被用进产出物：模板、图标、字体等
```

子目录名取自受控集合，**禁止单复数 / 同义词变体**：必须是 `scripts`、`references`（复数）、`assets`；禁止 `reference`（单数）、`refs`、`docs`、`script`、`static` 等写法。个别技能确有子代理 / 评测等特殊需求时，可自增目录（如 `agents/`、`evals/`）并在该技能内自述用途——属**例外、需克制**，不纳入通用受控集合。

## 三件套语义边界（必须遵守）

| 目录 | 一句话 | 放什么 | 不放什么 |
| --- | --- | --- | --- |
| `scripts/` | **被运行** | 确定性、可重复的可执行代码（脚本可直接执行，不必读入上下文） | 大段说明文档 |
| `references/` | **被阅读** | 需要时才读入上下文的深度文档、全量属性 / 路径表、扫描结果、对照速查 | 可执行脚本、最终产出素材 |
| `assets/` | **被用进产出物** | 模板、图标、字体等会出现在结果里的文件 | 说明性文档、脚本 |

> 记忆法：**scripts = 跑、references = 读、assets = 用。**

## SKILL.md 与 references/ 的分工

| 文件 | 放什么 | 不放什么 |
| --- | --- | --- |
| **SKILL.md** | `name` / `description`；何时用；**执行顺序与核心规则**；最少的关键锚点；短示例；执行清单；指向 `references/` 下文件的入口 | 长表格（全量属性表、全量路径清单、扫描结果）；易随时间漂移的快照数据 |
| **references/** | 路径 / 模块契约展开表；属性或样式**全表**；继承链 / 对照速查；扫描结果与维护步骤；与 SKILL 配套的索引 | 重复 SKILL 的完整流程；替代 SKILL 成为唯一入口 |

`SKILL.md` 正文应能**独立完成常见任务**；`references/` 仅在需要"核对事实、查全表、维护同步"时被读取。

## 何时应建 references/ 下的参考文件

满足**任一**即建议（或必须）拆出参考文件，并在 `SKILL.md` 开头说明"详情见参考文件"：

- 单表超过约 **15 行**，且会随对象演进而漂移（如某类全量属性 / 样式表）。
- 需记录**扫描结果**、官方子页清单、多文件路径索引等会变动的快照。
- 同一技能内"清单 + 大表"并存，拆出后 `SKILL.md` 能明显变短。

仅有流程说明、少量锚点、短列表时**不必**拆分；不要为形式而空建文件。

## 拆分的禁止项与组织建议

- **禁止**为图省事把参考文件里的大表整段复制回 `SKILL.md`；可在正文保留 3–5 行速查 + 一句"完整内容见参考文件"。
- 大型参考文件（> 300 行）应在开头放目录（TOC）。
- 技能支持多领域 / 多变体时，按变体拆分到 `references/<variant>.md`（如不同平台 / 不同框架各一份），正文只做"选择 + 流程"，让读者只读相关变体。
- 当 `references/` 下有**多个**文件时，在 `SKILL.md` 末尾放一个简短「参考文件」索引，逐项注明"何时去读"；单文件不必。

## 结构预检清单

- [ ] 存在 `SKILL.md`，以合法 YAML frontmatter（`---` 包裹）开头。
- [ ] 正文含执行流程 / 核心规则；整体 < 500 行（超出则下沉到 `references/`）。
- [ ] 子目录命名仅用受控集合（`scripts` / `references` / `assets`），无单数 `reference/` 等变体。
- [ ] 大表、全量清单、易漂移快照已放入 `references/`，未堆在 `SKILL.md`。
- [ ] `scripts/` 内为可执行代码并考虑跨平台；有依赖则附依赖清单。
- [ ] 多变体技能已按 `references/<variant>.md` 拆分。

---

# 综合执行流程

创建或重构一个 Skill 时按序进行：

1. **定族 + 起名**：判断是流程类（动名词）还是能力类（名词），按 Part 1 规则确定 `name`；目录名同步为该名。
2. **写 description**：同时表达 WHAT 与 WHEN，含触发词，略带推动性，≤ 1024 字符。
3. **写正文**：用祈使句描述执行顺序与核心规则，能独立完成常见任务；解释清楚"为什么"，少用全大写硬约束。固定产出格式时用**模板代码块**给出确切结构；给示例用 **`Input:` / `Output:`** 成对范式。
4. **评估是否拆分**：按 Part 2 的判据决定是否建 `references/` / `scripts/` / `assets/`；不为形式空建。
5. **跑预检清单**：依次过命名预检与结构预检；目录名必须等于 `name`。
6. **保持一致**：重命名技能时，同步修正配套规则 / 文档的词干，避免遗留旧名。

# 验证

完成后确认：

- 目录名、`name` 字段三处一致，且为合法 kebab-case（≤ 64 字符）。
- `description` 同时回答了"做什么"和"何时用"。
- `SKILL.md` 能被独立读懂并执行常见任务，没有把会漂移的大表塞进正文。
- 凡拆出的内容都落在受控子目录（`scripts/` / `references/` / `assets/`）中，命名无变体。
