apply {
    from rootProject.file(files.dev_lib_file)
}

android.defaultConfig {
    versionCode versions.dev_app_versionCode
    versionName versions.dev_app_versionName
}

dependencies {
    implementation deps.android.design
}