<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/vid_root_ll"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical">

    <!-- Status Bar -->

    <LinearLayout
        android:id="@+id/vid_status_bar_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

    <!-- Title -->

    <LinearLayout
        android:id="@+id/vid_title_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

    <!-- 内容区域块 -->

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- 内容主体 -->

        <FrameLayout
            android:id="@+id/vid_body_fl"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- 填充容器 -->

            <LinearLayout
                android:id="@+id/vid_content_ll"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical" />

            <!-- 状态布局容器 -->

            <LinearLayout
                android:id="@+id/vid_state_ll"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:visibility="gone" />
        </FrameLayout>

        <!-- 悬浮容器 -->

        <FrameLayout
            android:id="@+id/vid_float_fl"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>
</LinearLayout>