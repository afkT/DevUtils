apply {
    from rootProject.file(files.app_lib_gradle)
}

android.defaultConfig {
    versionCode versions.dev_other_versionCode
    versionName versions.dev_other_versionName
}

dependencies {
//    compileOnly project(path: ':DevJava')
}